export const savePath = (state, path_name) => {
    state.path_name = path_name;
}

export const setLoginFlag = (state, login_flag) => {
    state.login_flag = login_flag;
}

export const saveUsername= (state, username) => {
    state.username = username;
}